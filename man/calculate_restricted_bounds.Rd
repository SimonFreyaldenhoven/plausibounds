% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_restricted_bounds.R
\name{calculate_restricted_bounds}
\alias{calculate_restricted_bounds}
\title{Calculate Plausible Bounds}
\usage{
calculate_restricted_bounds(
  estimates,
  var,
  alpha = 0.05,
  preperiods = 0,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{estimates}{A vector of point estimates. If preperiods > 0, the first preperiods
elements are pre-treatment estimates, followed by post-treatment estimates.}

\item{var}{The variance-covariance matrix of the estimates}

\item{alpha}{Significance level (default: 0.05)}

\item{preperiods}{Number of pre-treatment periods (default: 0). Period 0 is assumed
to be normalized and not included in estimates.}

\item{parallel}{Whether to use parallel processing (default: FALSE)}

\item{n_cores}{Number of cores to use for parallel processing (default: NULL, which uses
detectCores() - 1). Only used when parallel = TRUE.}
}
\value{
A list containing:
\item{restricted_bounds}{A data frame with columns for horizon (event time),
unrestricted estimates, restricted estimates, and plausible bounds}
\item{Wpre}{Wald test for no pre-trends (statistic and p-value), if preperiods > 0}
\item{Wpost}{Wald test for no treatment effect (statistic and p-value)}
\item{metadata}{A list with metadata about the calculation}
}
\description{
This function calculates the plausible (or "restricted") bounds for a vector of estimates using model selection.
Supports pre-treatment periods for event study designs.
}
\keyword{internal}
